@{
    if (ViewBag.Categories != null)
    {

        foreach (var category in ViewBag.Categories)
        {
            <div class="dropdown-item">
                <div class="dropdown-item-text" onclick="ShopByCategory(@category.Id)" style="cursor:pointer">
                    @category.Name
                </div>
            </div>
        }

    }
}