@model IEnumerable<DQueensFashion.Models.ViewCategoryViewModel>

@{
    ViewBag.Title = "Categories";
    Layout = "~/Views/Shared/_dashboardLayout.cshtml";
}


<div class="row">
    <div class="col-md-2"></div>
    <div class="col-md-8">
        <div>
            <div class="category-table-add-category-btn">
                <a href="@Url.Action("AddCategory")" class = "btn btn-sm">
                    <img src="~/Content/Images/plus.svg" />
                    Add new
                </a>
                
            </div>
        </div>
        
        <div class="card">
            <div class="card-header">
                <h3 class="card-title">Categories</h3>


                <div class="card-tools">

                    <div class="input-group input-group-sm">
                        <input type="text" name="table_search" class="form-control float-right" id="search" placeholder="Search">

                        <div class="input-group-append">
                            <button type="submit" class="btn btn-default table-search-btn" onclick="SearchCategoriesTable()">
                                <i class="fas fa-search"></i>
                            </button>
                        </div>
                    </div>

                </div>

            </div>
            <!-- /.card-header -->
            <div class="card-body table-responsive p-0 custom-table">

                @Html.Partial("_categoriesTable", Model)
            </div>
            <!-- /.card-body -->
        </div>
        <!-- /.card -->
    </div>
</div>


<script>
        var search = document.getElementById("search");

    function SearchCategoriesTable() {
         $.ajax({
            url: '/Admin/SearchCategories',
            dataType: "html",
            data: { query: search.value },
            success: function (result) {

                $("#categoriesTable").html(result);
            },
            error: function (xhr, status, error) {
                alertify.error("Error");
            }
        });
    }
</script>